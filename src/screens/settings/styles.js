import { StyleSheet } from 'react-native';

const styles = (theme) => StyleSheet.create({
    container: {
        flex: 1
    },

    item: {
        padding: 16,
        width: '100%',
        height: 54
    },

    divider: {
        width: '100%'
    }
});

export default styles;