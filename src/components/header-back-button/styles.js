import { StyleSheet } from 'react-native';

const styles = (theme) => StyleSheet.create({
    icon: {
        width: 32,
        height: 32,
        marginLeft: 4,
        marginRight: 4
    },
});

export default styles;