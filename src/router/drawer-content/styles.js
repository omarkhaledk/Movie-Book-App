import { StyleSheet } from 'react-native';

const styles = (theme) => StyleSheet.create({
    drawerContent: {
        height: '100%',
        paddingTop: 14
    },
    drawerItem: {
        fontSize: 16
    },
    divider: {
        marginBottom: 18
    }
});

export default styles;
